#include <iostream>
#include <fstream>

using namespace std;

//Use network flow, minimizing paths to G1

int main() {
	ifstream cin("gangs.in");
	ofstream cout("gangs.out");

	int num_cows, num_gangs;
	cin >> num_cows >> num_gangs;
	string order = "";
	for (int i = 0; i < num_gangs; ++i) {
		int 
		cin >> gang_size[i];
		for (int j = 0; j < gang_size[i])
	}	
}