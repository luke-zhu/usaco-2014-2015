#include <iostream>
#include <fstream>

using namespace std;

int n, pos[100][2], min_len[100];

int breadth_first_search(int start, int curr_x, int curr_y)
		
}

int queue

int main() {
	ofstream fout("delivery.out");
	ifstream fin("delivery.in");

	fin >> n;
	for (int i = 0; i < n; ++i) {
		fin >> pos[i][0] >> pos[i][1];
	}
}